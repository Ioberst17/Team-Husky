using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UIInventory : MonoBehaviour
{
    // Purpose of UIInventory is to help keep track of items in the UI
    // It does this by updating the text UI buttons; its script is attached to the text of the inventory button


    void Start()
    {

    }

    // Update is called once per frame
    void AddItems(int numberToAdd)
    {
        
    }

}
